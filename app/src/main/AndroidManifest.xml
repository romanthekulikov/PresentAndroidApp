<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_db"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_db_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Present"
        tools:targetApi="31">
        <activity
            android:name=".activities.DistributiveActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.welcomePack.WelcomeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.getPresentPack.GetPresentActivity"
            android:exported="false" />
        <activity
            android:name=".activities.rulesPack.RulesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.mapPack.MapActivity"
            android:exported="false" />
        <activity
            android:name=".activities.presentPack.PresentActivity"
            android:exported="false" />
        <activity
            android:name=".activities.mainPack.MainActivity"
            android:exported="false" />
    </application>

</manifest>